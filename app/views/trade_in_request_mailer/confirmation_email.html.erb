<div
  style="
    font-family: sans-serif;
    color: #33333A;
    padding: 0 30px 30px 30px;
    text-align: center;
    line-height: 1.5;
  "
>
  <h2 style="font-size: 28px;">Trade-in request confirmed!</h2>
  <div
    style="
      background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url('<%= asset_path 'tradein3.jpeg' %>');
      background-size: cover;
      background-position: center;
      padding: 30px 20px;
      margin: 30px 0;
    "
  >
    <div style="color: white;">
      <% if @trade_in_request.with_promo %>
        <p>Realize your Trade-in in one of Salva Mi Máquina shops and</p>
        <p>enjoy a <b>10% discount on all accessories!</b></p>
        <p>with this coupon code:</p>
        <span
          style="
            display: inline-block;
            padding: 10px 30px;
            margin: 15px 0;
            color: white;
            background-color: #39ab4b;
            border: 1.5px solid white;
            font-weight: bold;
          "
        >
          <%= @trade_in_request.coupon_code %>
        </span>
        <p><b>This coupon is valid only <%=  @coupon_validity_days %> days!</b></p>
        <p>(You have until <%= I18n.localize((@trade_in_request.created_at + @coupon_validity_days.days), format: :simple).titleize %> to use it)</p>
        <p><small><i>Not valid on our Ecommerce</i></small></p>
      <% else %>
        <p>Realize your Trade-in in one of Salva Mi Máquina shops</p>
      <% end %>
    </div>
  </div>

  <hr style="height: 1px; background-color: rgba(0, 0, 0, .125); margin: 30px 0; opacity: 1; border: none;" />

  <div>
    <h2 style="font-size: 28px;">Your Trade-in</h2>
    <div>
      <p>
        <%= t('you_have_a') %> <b><%= @trade_in_model.name %></b>
      </p>
      <p>
        (<%= t('estimated_value_between') %> <b style="color: #39ab4b;"><%= number_to_currency(@trade_in_model.min_value) %></b> <%= t('and') %> <b style="color: #39ab4b;"><%= number_to_currency(@trade_in_model.max_value) %></b>)*
      </p>
    </div>
  </div>

  <div style="margin-top: 30px;">
    <div style="margin-bottom: 30px; fill: #33333A;">
      <%= svg 'arrow_down_circle' %>
    </div>

    <div>
      <div style="display: flex; align-items: center; justify-content: center; padding: 30px; background-color: #d8fcdb;">
        <div>
          <%= t('you_can_by_a') %> <b><%= @variant.name %></b> - <b><%= @variant.options_text %></b>
          <br />
          <%= t('between') %> <b style="color: #39ab4b;"><%= number_to_currency(@variant.price - @trade_in_model.min_value.to_f) %></b> <%= t('and') %> <b style="color: #39ab4b;"><%= number_to_currency(@variant.price - @trade_in_model.max_value.to_f) %></b>
        </div>
      </div>
      <div style="margin: 20px 0;">
        <%= cl_image_tag_with_folder(@variant.images.first&.attachment, width: 200, crop: :fill, model: Spree::Image) %>
      </div>
      <p>
        <i>(<%= t('instead_of') %> <b><%= number_to_currency(@variant.price) %></b>)</i>
      </p>
    </div>
  </div>
  <p><i>*<%= t('trade_in_disclaimer') %></i></p>
</div>