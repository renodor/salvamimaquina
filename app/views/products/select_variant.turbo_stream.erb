<%= turbo_stream.replace 'product-images' do %>
  <%= render 'product_images', variant: @variant %>
<% end %>

<%= turbo_stream.replace 'add-to-cart-form' do %>
  <%= render 'add_to_cart_form', variant: @variant %>
<% end %>

